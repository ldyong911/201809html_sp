<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap
			PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"
			"http://ibatis.apache.org/dtd/sql-map-2.dtd">
<!-- namespace속성 : 아래에서 작성한 SQL을 호출할 때 같이 사용된다. -->			
<sqlMap namespace="omok">
	<!-- 
		위 태그에서 사용되는 속성들
	 	1) id속성 : 해당 태그를 호출할 때 <sqlMap>태그의 namespace와 연결하여 사용하는 이름
	 	2) parameterClass속성 : sql문에 사용될 데이터가 들어있는 객체를 지정한다. 
	 						   (보통 VO클래스, 자바의 자료형이름 등이 사용된다.)
	 						   (typeAlias로 지정한 alias명을 사용할 수 있다.)							  
		3) resultClass속성 : select문을 실행한 결과를 담을 객체를 지정한다.
							(보통 VO클래스나 자바의 자료형이름을 사용한다.)							 
	-->
	
	<!-- 
	parameterClass에 설정되는 값이 단일값(즉 1개의 데이터)이면
	SQL문에 이 값을 나타내는 변수는 특별히 이름이 정해지지 않았다.
	(즉, 사용자가 임의로 지정할 수 있다.)
	-->
	 
	<update id="insert" parameterClass="omokVO">
		insert into omok (pan, pan_order, history, winner)
	 	values(#pan#, #pan_order#, #history#, #winner#)
	</update>

	<select id="selectList" resultClass="omokVO">
		select pan, pan_order, history, winner
		from omok
	</select>
	
	<select id="selectMax" resultClass="omokVO">
		select idx_omok.nextval as pan
		from dual
	</select>
	
	<select id="selectListPan" resultClass="omokVO">
		select distinct pan
		from omok
		order by pan
	</select>
	
	<select id="selectListPans" resultClass="omokVO" parameterClass="omokVO">
		select pan, pan_order, history, winner
		from omok
		where pan = #pan# order by pan_order
	</select>
	
	<select id="selectListHist" resultClass="omokVO" parameterClass="omokVO">
		select pan, pan_order, history, winner
		from omok
		where history = #history#
	</select>
	
	<select id="selectListHistNext2" resultClass="omokVO" parameterClass="omokVO">
		select pan, pan_order, history, winner
		from omok
		where pan = #pan#
		and pan_order = #pan_order#
	</select>
	
</sqlMap>